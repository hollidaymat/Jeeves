# Anthropic API Key (required for local development)
# Get your key from: https://console.anthropic.com/
# Note: When deployed to Vercel, the AI Gateway handles this automatically
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Signal Configuration (for Phase 2 - Linux only)
SIGNAL_PHONE_NUMBER=+1234567890

# Server Configuration
PORT=3847
HOST=127.0.0.1

# HTTPS (optional) – set both to enable TLS so browser mic works from any client
# Generate self-signed: openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes -subj "/CN=192.168.7.50"
# TLS_KEY_PATH=/path/to/key.pem
# TLS_CERT_PATH=/path/to/cert.pem
# Optional: trust a self-signed cert (e.g. Nextcloud). Run scripts/fetch-nextcloud-cert.sh then set:
# NODE_EXTRA_CA_CERTS=/path/to/signal-cursor-controller/config/nextcloud-server.pem

# qBittorrent (homelab integration)
QBITTORRENT_URL=http://192.168.7.50:8085
QBITTORRENT_USER=jeeves
QBITTORRENT_PASS=whyaskjeeves

# Voice (optional) – STT for hold-to-talk and "Hey Jeeves"
# VOICE_ENABLED=true
# PIPER_URL=http://127.0.0.1:10200     # Wyoming Piper TTS (TCP)
# PIPER_VOICE=en_GB-alan-medium        # British male (Alan). Other en_GB: northern_english_male, aru. Female: en_GB-alba-medium
# WHISPER_URL=http://127.0.0.1:10300   # Wyoming Whisper STT (TCP) or HTTP with /api/speech-to-text
# VOICE_WAKE_MODEL_PATH=               # optional; default: models/wake/hey_jeeves.onnx (see models/wake/README.md)

# Antigravity Orchestrator (optional – "build X" / "orchestrate X" / "antigravity build X" flow)
# First-time setup: install Antigravity, log in once with Google (same user as Jeeves), then set vars below.
# See docs/ANTIGRAVITY_ORCHESTRATOR_SETUP.md for step-by-step (no QA browser required).
# Recommended: Jeeves = your employee (Anthropic API above). Antigravity = coding only (its own LLMs).
# With ANTIGRAVITY_USE_CHAT=true, the coding step runs inside Antigravity and uses Antigravity's
# built-in LLMs (e.g. Gemini). Jeeves uses Anthropic for PRD/spec/validation (do not set ANTHROPIC_BASE_URL).
# ANTIGRAVITY_USE_CHAT=true                   # use "antigravity chat --mode agent" for coding (Antigravity's LLMs)
# ANTIGRAVITY_SERVE_WEB=true                 # spawn antigravity serve-web for QA browser (requires antigravity-tunnel; Linux APT pkg does not include it)
# ANTIGRAVITY_SERVE_WEB_PORT=3010           # port for serve-web (default 3010; 3000 is often Grafana)
# WSL2→Windows browser tunnel: see docs/ANTIGRAVITY_WSL2_BROWSER_TUNNEL.md (socat TCP 9222 to WIN_IP:9222)
# ANTIGRAVITY_HANDOFF_ONLY=true               # write spec only, no build (test Jeeves–Antigravity handoff)
# ANTIGRAVITY_STUB=true                       # skip CLI, return stub success (for testing)
# ANTIGRAVITY_MODEL=...                       # only for hypothetical "antigravity execute" path
# TASK_TEMP_DIR=/tmp/antigravity_tasks
# TASK_OUTPUT_DIR=/tmp/antigravity_output
# MAX_ITERATIONS=3
# PLAYBOOK_UPDATE_INTERVAL_MS=300000         # 5 min
# Advanced: ANTHROPIC_BASE_URL only if you explicitly route LLM calls through a proxy (optional, not recommended).

# Logging
LOG_LEVEL=info
